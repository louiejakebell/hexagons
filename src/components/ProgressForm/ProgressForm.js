import React from 'react';

import Button from '../Styled/Button';
import Input from '../Styled/Input';

const ProgressForm = ({ activeTab, save }) => {
  let input;

  return (
    <div>sdf</div>
  );
};

export default ProgressForm;
